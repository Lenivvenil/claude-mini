# feat(verifier): Schemathesis for any HTTP surface in pet-projects

**Priority:** P2
**Estimate:** M
**Depends on (in this batch):** —

## Problem statement

news-digest bot имеет HTTP surface (если webhook). Schemathesis (v4.16.1, апрель 2026) — drop-in OpenAPI fuzzing на Hypothesis engine; phases examples → coverage → fuzzing → stateful.

## Acceptance criteria

- [ ] Schemathesis установлен в каждом проекте с HTTP surface.
- [ ] pytest integration: `schema.parametrize()` decorator.
- [ ] CI workflow: `schemathesis/action@v3` с `examples + coverage + fuzzing` phases.
- [ ] Stateful только если OpenAPI spec имеет links.
- [ ] Bounded `--max-examples=200` для CI runtime.

## Non-goals

НЕ для проектов без HTTP API.

## References

Принцип 8. schemathesis.io.

Synthesis: `docs/synthesis/2026-04-29-pipeline-restructuring.md` (Раздел B, ticket #22).
Principles touched: #8.
