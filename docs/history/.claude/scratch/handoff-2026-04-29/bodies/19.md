# feat(verifier): semgrep rules for hedging-words and corporate-softening in plan.md

**Priority:** P1
**Estimate:** S
**Depends on (in this batch):** —

## Problem statement

Gap 4 (banned terms in plan.md). Принцип 1 («размытость — нарушение») requires mechanical detection of hedging language: `maybe`, `possibly`, `depends`-without-branching-condition, «could», «might», «perhaps».

## Acceptance criteria

- [ ] `.semgrep/hedging.yml` rules с regex'ами на banned terms в `plan.md`, `STATE.md`, `docs/decisions/*.md`.
- [ ] Pre-commit hook вызывает semgrep на staged markdown files.
- [ ] `depends` allowed только если followed by «when X then Y, when Z then W» branching.
- [ ] False-positive review process: добавление в `.semgrepignore` с reason-comment.

## Non-goals

НЕ generic prose-linter; НЕ грамматика.

## References

Принцип 1.

Synthesis: `docs/synthesis/2026-04-29-pipeline-restructuring.md` (Раздел B, ticket #19).
Principles touched: #1.
